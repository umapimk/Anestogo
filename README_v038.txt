Anesthculator v0.38 — Back Navigation Fix

Changes from v0.37:
- Prominent ← Back button in sticky top header.
- In-app Back returns to the previous app view without page reload, preserving patient inputs and local state.
- Back closes an open dialog first.
- Library guide remains collapsed by default.
- Dosing Weight Basis support from v0.37 retained.
- Service-worker cache bumped to v0.38 to prevent GitHub Pages from serving stale v0.37 UI.
